_schema-version: "3.2"
ID: commindsetadrapapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: commindsetadrapapp-destination-content
  type: com.sap.application.content
  requires:
  - name: commindsetadrapapp-destination-service
    parameters:
      content-target: true
  - name: commindsetadrapapp-repo-host
    parameters:
      service-key:
        name: commindsetadrapapp-repo-host-key
  - name: commindsetadrapapp-uaa
    parameters:
      service-key:
        name: commindsetadrapapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: commindsetadrapapp_html_repo_host
          ServiceInstanceName: commindsetadrapapp-html5-srv
          ServiceKeyName: commindsetadrapapp-repo-host-key
          sap.cloud.service: commindsetadrapapp
        - Authentication: OAuth2UserTokenExchange
          Name: commindsetadrapapp_uaa
          ServiceInstanceName: commindsetadrapapp-xsuaa-srv
          ServiceKeyName: commindsetadrapapp-uaa-key
          sap.cloud.service: commindsetadrapapp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: commindsetadrapapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: commindsetadrapapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - commindsetadrapapp.zip
      name: commindsetadrapapp
      target-path: resources/
- name: commindsetadrapapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: commindsetadrapapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: commindsetadrapapp-destination-service
    service-plan: lite
- name: commindsetadrapapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: commindsetadrapapp-xsuaa-srv
    service-plan: application
- name: commindsetadrapapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: commindsetadrapapp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
